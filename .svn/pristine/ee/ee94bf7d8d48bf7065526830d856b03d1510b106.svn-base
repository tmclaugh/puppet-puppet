# $Id$

# XXX: Shifty hack to get Jenkin going. This should become a full
# fetured module at some point for handling both std. vendor jdk
# packages and non-std. packages.

class java ($version="1.6.0") {
	package {"java-${version}-openjdk":
		ensure => installed,
	}
}
