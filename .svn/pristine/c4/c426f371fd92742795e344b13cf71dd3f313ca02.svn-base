# $Id$
#
# Install PHP and modules.
#

class apache::php {
	include apache::httpd

	package { "php" :
		ensure => "installed"
	}
}

define apache::php::module () {
	package { "php-${name}" :
		ensure => "installed",
		require => Package["php"],
		notify => Service["httpd_service"]
	}
}